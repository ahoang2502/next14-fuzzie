import React from "react";

const EditorPage = () => {
  // WIP redirect to editorId or prompt to create a workflow or send back to workflow page
  return <div>EditorPage</div>;
};

export default EditorPage;
